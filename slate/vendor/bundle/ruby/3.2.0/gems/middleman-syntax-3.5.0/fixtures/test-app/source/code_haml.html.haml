%h1 Ruby

= code('ruby') do
  :preserve
    def foo(bar)
      puts "baz"
    end

%h1 Whatever

= code do
  This is some code

%h1 Options

= code('ruby', {:line_numbers => true, :start_line => 7}) do
  :preserve
    def foo(bar)
      puts "baz"
    end