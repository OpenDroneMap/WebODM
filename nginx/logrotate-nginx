/var/log/nginx/*.log {
    daily
    missingok
    rotate 1
    delaycompress
    compress
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        if [ -f /tmp/nginx.pid ]; then
            kill -USR1 `cat /tmp/nginx.pid`
        fi
    endscript
    maxage 14
}